# Docspress WordPress plugins

WordPress plugins maintained as part of Docspress live here, with one subdirectory per plugin.

- [`docspress-blocks/`](docspress-blocks/) provides documentation-focused Gutenberg blocks, starter patterns, and **modules** (Mantle-style feature folders under `includes/Modules/` + `src/modules/`). The first module is **Pages List** — an opt-in hierarchical DataViews Pages screen for deep documentation trees.

This directory and the sibling `theme/` directory are repository distributions. They are intentionally excluded from the `docspress` npm package.
